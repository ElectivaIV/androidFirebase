<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@id/download">

    <fragment
        android:id="@+id/download"
        android:name="com.example.musicplayer.Download"
        android:label="fragment_download"
        tools:layout="@layout/fragment_download" />
    <fragment
        android:id="@+id/configuration"
        android:name="com.example.musicplayer.Configuration"
        android:label="fragment_configuration2"
        tools:layout="@layout/fragment_configuration2" />
    <fragment
        android:id="@+id/playMusic"
        android:name="com.example.musicplayer.PlayMusic"
        android:label="fragment_play_music"
        tools:layout="@layout/fragment_play_music" />
</navigation>